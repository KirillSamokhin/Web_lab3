import{router}from"./router.js";import express from"express";import{fileURLToPath}from"url";import{dirname,join}from"path";let app=express();const currentFileUrl=import.meta.url,currentFilePath=fileURLToPath(currentFileUrl),currentDirectory=dirname(currentFilePath);app.set("view engine","pug"),app.set("views",join(currentDirectory,"./src/views")),app.use(express.static(join(currentDirectory,"src"))),app.use("/",router),app.set("port","3000"),app.listen("3000");